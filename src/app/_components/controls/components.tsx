"use client";
import { observer } from "mobx-react-lite";

const Components = () => {
  return (
    <div>
      <h3>Add Elements</h3>
      <div></div>
    </div>
  );
};

export default observer(Components);
